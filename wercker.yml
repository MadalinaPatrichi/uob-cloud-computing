box: openjdk:8

build:
  steps:
    - script:
        name: app gradle build
        cwd: app
        code: ./gradlew test
        code: ./gradlew build

deploy-docker:
  steps:
    - script:
        name: deploy docker
        cwd: app
        code: echo noop

deploy-docs:
  steps:
    - script:
        name: build and deploy docs
        cwd: app
        code: echo noop

deploy-jar:
  steps:
    - script:
        name: deploy jar
        cwd: app
        code: echo noop
